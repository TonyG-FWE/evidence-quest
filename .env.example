# Optional Node service configuration; never use VITE_ for secrets.
HOST=127.0.0.1
PORT=8787
PUBLIC_ORIGIN=http://127.0.0.1:5173
COACH_MODE=authored

# After explicit paid synthetic/adult evaluation authorization only:
# COACH_MODE=adult-evaluation
# OPENAI_MODEL=gpt-6-astra
# Put OPENAI_API_KEY in ignored .env.server.local, never in client configuration.
