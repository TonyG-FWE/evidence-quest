import {readFile,writeFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';

// Read-only asset inspection. No production image pixels or imported plans change.
const hash=bytes=>createHash('sha256').update(bytes).digest('hex');
const planPath='docs/design/evidence-quest-design-v3/11-build-packet/implementation-plan.json';
const manifestPath='docs/design/evidence-quest-design-v3/10-asset-production/asset-manifest.json';
const planBytes=await readFile(planPath),manifestBytes=await readFile(manifestPath);
const plan=JSON.parse(planBytes),manifest=JSON.parse(manifestBytes);
const replacements=JSON.parse(await readFile('content/illustrated-assets.json','utf8'));
const inventory=JSON.parse(await readFile('evidence/er13/art-runtime-inventory.json','utf8'));
const images=[];
for(const entry of inventory.files){
 const bytes=await readFile('public'+entry.url);
 if(hash(bytes)!==entry.sha256||bytes.length!==entry.bytes)throw Error('Image inventory drift: '+entry.url);
 if(bytes.subarray(1,4).toString()!=='PNG')throw Error('Expected PNG source');
 const width=bytes.readUInt32BE(16),height=bytes.readUInt32BE(20);
 images.push({...entry,width,height,rgbaBytes:width*height*4});
}
const imageMap=new Map(images.map(image=>[image.url,image]));
const incompleteBindings=manifest.assets.filter(asset=>asset.representation==='raster').flatMap(asset=>{
 const variants=asset.variants.filter(variant=>!replacements[variant.id]).map(variant=>variant.key);
 return variants.length?[{assetId:asset.id,variants,meaning:'No direct accepted-image override. See runtime exceptions; this alone is not a claim of a visible missing object.'}]:[];
});
const homeKeys=['ASSET.ENV.ST.BACKPLATE/base','ASSET.ACT.JO/talk','ASSET.ACT.REMY/home','ASSET.ACT.ARI/home','ASSET.PUP.PIP/waiting','ASSET.PUP.GRANDMA/waiting'];
const homeImages=[...new Set(homeKeys.map(key=>replacements[key].url))].map(url=>imageMap.get(url));
const statuses=[
 {id:'TASK11.17',status:'COMPLETE',implemented:'Seven planned comparison/timeline/private-idea states and eleven transitions; private immutable revisions, exact component ownership, displayed metadata, optional relations, explicit Help and separate Go.',remaining:[],evidence:['docs/TASK11.17.md','evidence/er13/task17-acceptance.json']},
 {id:'TASK11.18',status:'COMPLETE_LOCAL_SYNTHETIC',implemented:'Real native Node/fetch Responses adapter, same-origin HTTP client, eligible source reconstruction, schema projection, refusal/fallback, bounds, request ownership and timeout. Authored public default remains active.',remaining:['No live claim: configuration/account authorization and semantic interpretation evaluation belong to TASK11.19.'],evidence:['server/coach.ts','server/index.ts','src/coach/http-transport.ts','checks/er13.test.ts','evidence/er13/task17-contracts.txt','browser-tests/er13.spec.ts','docs/ER13-CORRECTION.md']},
 {id:'TASK11.19',status:'NOT_RUN',implemented:'Evaluation fixtures and authorization boundary retained.',remaining:['Explicit paid synthetic/adult evaluation authorization, server-only credential and access to selected model, then recorded live trials. Zero live calls; child-live remains disabled.'],evidence:['docs/ER13-CORRECTION.md']},
 {id:'TASK11.20',status:'PARTIAL',implemented:'All 36 accepted sources plus Stage are byte-verified and integrated; runtime anchors, text/source fidelity, visible story and Toast consequences reviewed.',remaining:['Required temporary doors, small carriers, petals, bridge/join pieces, tile faces and E2 photo remain. See exact override inventory and runtime exceptions.','No prescribed WebP/PNG base/2x production export set or validated per-group size allocation. Current replacement loader forces source density 1.','Assets caches each requested image by URL for its owner lifetime; no byte-budgeted density eviction/adjacent-room policy exists.','Initial transfer, actual decoder/GPU/cache use, five-second start, 60/30 fps and input targets have not been measured on the required hardware. Source-file arithmetic below already exceeds the opening transfer target.'],evidence:['evidence/er13/art-runtime-inventory.json','src/world/assets.ts','src/world/paint.ts','evidence/er13/review-ledger.md']},
 {id:'TASK11.21',status:'PARTIAL_DEPENDENCY_OPEN',implemented:'29 contracts, all65/five successful orders,36 cue-race variants, connected production/browser fault tests and focused three-engine TASK11.17 checks; native saved premiere and final art paths reviewed.',remaining:['TASK11.20 dependency remains partial.','No completed final illustrated matrix covering every96 state/121 transition and15 flows across all3 engines, desktop/compact/Largest/reduced-motion on one qualified candidate.','Physical touch hardware, screen-reader/device coverage and all-device durability remain unqualified. Existing synthetic and native evidence are labeled separately.'],evidence:['evidence/first-connected-acceptance.md','evidence/er13/checkpoint-evidence.json','evidence/er13/task17-acceptance.json','evidence/er13/parent-native-review.md']},
 {id:'TASK11.ART02',status:'PARTIAL',implemented:'Red backpack motif, current white Jo, crew narrative poses, player right/front/back walk sheets with mirrored left, one rich Loop and visible carried caddy.',remaining:['Original61 world-actor cel specification not fulfilled: player reach variants absent; crew face/receive/acknowledge variants absent or unused; renderer currently selects home/talk.','Loop states reuse one accepted drawing with native lens/wheel overlays, not the original rolling/responsive/docking/projecting cel set. Carry reuses walking frames.','Original no-mirrored-asymmetries and every-pose production review not established.'],evidence:['content/illustrated-assets.json','src/world/paint.ts','evidence/er13/accepted-art-delivery-notes.md']},
 {id:'TASK11.ART03',status:'PARTIAL',implemented:'All four rich empty backplates integrated; fixed collision/approach geometry remains authoritative. Parent explicitly accepts density derivatives bounded by actual source detail, superseding the original suggested2880px dimension.',remaining:['Three doorway variants remain temporary.','Measured density derivatives from the genuine1536x1024 room sources remain to be produced; no upscale is claimed as new detail.'],evidence:['content/illustrated-assets.json','src/world/paint.ts']},
 {id:'TASK11.ART04',status:'PARTIAL',implemented:'Accepted room furniture, open/closed caddy and note/envelope atlas, all six Toast components; one physical owner and source-access identity retained through movement/handoff.',remaining:['Model tab, brief flap, note drawer, device outside the included Courtyard tablet, dock flap, curled/moving/flat notice and clip, slate and petals still use temporary artwork.','Several physical opening operations have controlled durations and settled endpoint changes, but not the original continuous hinge/slide/reach visual choreography.'],evidence:['src/world/paint.ts','evidence/er13/art-delivery-batch5.json']},
 {id:'TASK11.ART05',status:'PARTIAL',implemented:'Accepted material crops, Pip/Grandma endpoint poses, backpack, seed, boat, roots, bud/flower; real seed-only/crossing/planting/growth outcomes and both banks reviewed.',remaining:['Broken-bridge halves and joined connector remain temporary. Terrain retains temporary underpaint beneath separately clipped accepted texture.','Original complete production export/anchor/28-clip endpoint review remains ART07 work.'],evidence:['src/world/paint.ts','evidence/er13/watch-keyboard-three-engine.json','evidence/er13/parent-native-review.md']},
 {id:'TASK11.ART06',status:'PARTIAL',implemented:'E2 recording composes accepted Loop with native wood cart/door at original frame coordinates; frame1 and ending exposure stay distinct. Parent explicitly accepts this final composed format after native review. Photograph only exposes CANCELED; four tiles remain equal-status.',remaining:['Four tile faces and frozen partial photo remain original temporary art.','Production export/budget review remains open for the new faces/photo; no alternate E2 recording artwork is required.'],evidence:['src/ui/RecordingFrame.tsx','src/ui/Reader.tsx','browser-tests/sources-payoff.spec.ts']},
 {id:'TASK11.ART07',status:'PARTIAL',implemented:'Accepted source hashes, explicit uneven crop rectangles, supplied anchors, alpha metadata and representative native compositions verified.',remaining:['Base/2x packing, clear-padding/alpha-fringe audit and per-export byte caps are incomplete. All37 source PNGs are copied intact.','No exhaustive28-clip production review. Player reach/crew turn-celebration and physical carrier choreography do not yet implement every original presentation endpoint/transition.'],evidence:['scripts/import-er13-art.mjs','evidence/er13/art-runtime-inventory.json','evidence/er13/review-ledger.md']},
 {id:'TASK11.ART08',status:'NOT_STARTED_OPTIONAL',implemented:'Silent gameplay and optional device-local reading voice work.',remaining:['Six original foley masters and MP3 exports, specified fades/levels/durations and matching effect playback are unproduced. Local speech is not foley.'],evidence:['docs/PROVENANCE.md','evidence/er13/native-speech.json']},
 {id:'TASK11.ART09',status:'PARTIAL_OPTIONAL',implemented:'Native contact shadows, restrained petal rotation, Toast light/cover/arm motion and transparent magnifier aperture; reduced motion keeps settled factual information.',remaining:['Full optional flourish/lighting review depends on ART07; no complete final effects/occlusion qualification.'],evidence:['src/world/paint.ts','evidence/er13/final-watch-toast-checks.json']},
];
const tasks=statuses.map(status=>{
 const task=plan.tasks.find(task=>task.id===status.id);if(!task)throw Error('Unknown existing task '+status.id);
 return {...status,title:task.title,optional:task.optional,dependsOn:task.dependsOn,originalCompletionBoundary:task.completionBoundary};
});
const data={recordedAt:new Date().toISOString(),authority:[{path:planPath,sha256:hash(planBytes)},{path:manifestPath,sha256:hash(manifestBytes)}],purpose:'Reconcile completed ER13 integration with the unchanged original task acceptance boundaries. No newly invented task IDs or downgrade of the approved quality target.',tasks,acceptedSources:{files:images.length,bindings:Object.keys(replacements).length,encodedBytes:images.reduce((n,i)=>n+i.bytes,0),calculatedRgbaBytes:images.reduce((n,i)=>n+i.rgbaBytes,0),images},openingArtFloor:{basis:'The six original PNG files directly referenced by visible Home art; encoded disk bytes and width*height*4 arithmetic, excluding code, CSS, other art, browser overhead and timing. This is not measured network/decode performance.',files:homeImages.map(image=>image.url),encodedBytes:homeImages.reduce((n,i)=>n+i.bytes,0),calculatedRgbaBytes:homeImages.reduce((n,i)=>n+i.rgbaBytes,0),originalInitialTransferTargetBytes:manifest.budget.targets.initialTransferBytes},noDirectAcceptedOverride:incompleteBindings,runtimeExceptions:['Accepted CADDY/open|closed includes the lid and blank pockets; old CADDY.BODY/LID/pocket fallback is bypassed when accepted art is present.','Accepted Toast lid/base is moved natively; old closed/raised variants are bypassed.','E2.CLIP is drawn by RecordingFrame from accepted Loop plus native geometry; unmatched registry keys are not the current recording glyph.','CY stand includes its tablet; duplicate CY device and WK sign readable-face drawings are omitted.','Terrain still loads temporary underpaint before accepted texture clipped to identical polygons.','Missing crew/player reach keys are not currently selected by the renderer; that is an original-pose implementation gap rather than a disappearing character.'],nextExistingWork:'After this checkpoint, reconcile/deliver the unmet required ART02–07 items, then finish TASK11.20 export/loading/performance and TASK11.21 final matrix. TASK11.19 requires separate explicit live authorization/configuration; ART08/09 retain optional status.'};
await writeFile('evidence/er13/original-plan-reconciliation.json',JSON.stringify(data,null,2)+'\n');
const mib=value=>(value/1048576).toFixed(2);
await writeFile('docs/ER13-REMAINING-SCOPE.md',`# Original-plan completion reconciliation\n\nThe accepted ER13 delivery is fully installed and reviewed in the running game. It does not by itself complete original TASK11.20 or ART02–07. This audit preserves the approved visual target and existing task IDs. [Exact inputs, image sizes and remaining variants](../evidence/er13/original-plan-reconciliation.json).\n\n| Existing task | Status | Concrete remaining work |\n|---|---|---|\n${tasks.map(task=>`| ${task.id} | ${task.status} | ${task.remaining.join(' ').replaceAll('|','/')} |`).join('\n')}\n\nThe37 delivered PNG files total **${mib(data.acceptedSources.encodedBytes)} MiB** on disk. Their RGBA dimensions sum to **${mib(data.acceptedSources.calculatedRgbaBytes)} MiB** before browser overhead. This is source-size arithmetic, not concurrent decoded memory. The six images directly used by Home alone total **${mib(data.openingArtFloor.encodedBytes)} MiB**, above the original4 MiB initial transfer target even before code/CSS. The loader currently retains requested images by URL and has no budgeted eviction or production density switch. These are concrete optimization/export gaps, not merely an unrun benchmark.\n\nDo not count unmatched binding names as absent runtime objects without consulting the exceptions: the accepted caddy includes its lid/pockets, Toast uses its moved base lid, and E2 uses a native composed recording. The screenshot reviews establish visible endpoint behavior, not all original cel, export, clipping, performance, audio or hardware acceptance.\n\nNext: ${data.nextExistingWork}\n`);
console.log(JSON.stringify({tasks:tasks.map(({id,status})=>({id,status})),pngs:images.length,sourceMiB:mib(data.acceptedSources.encodedBytes),homeMiB:mib(data.openingArtFloor.encodedBytes)}));
