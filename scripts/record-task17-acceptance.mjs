import {readFile,writeFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';
const ledger=JSON.parse(await readFile('evidence/er13/checkpoint-evidence.json','utf8'));
const plan=JSON.parse(await readFile('docs/design/evidence-quest-design-v3/11-build-packet/implementation-plan.json','utf8'));
const task=plan.tasks.find(task=>task.id==='TASK11.17');
const outcomes=ledger.latestOutcomes.filter(outcome=>outcome.file.endsWith('reasoning.spec.ts'));
if(outcomes.length!==21||outcomes.some(outcome=>outcome.status!=='passed'))throw Error('TASK11.17 requires seven named checks in each of the three engines');
const evidence=['checks/reasoning.test.ts','browser-tests/reasoning.spec.ts','evidence/er13/task17-contracts.txt','evidence/er13/task17-final-regression.json','evidence/er13/parent-native-review.md','src/core/reasoning.ts','src/ui/Reasoning.tsx','src/core/reducer.ts','src/save/validate.ts','contracts/contracts.schema.json'];
const files=[];for(const path of evidence){const bytes=await readFile(path);files.push({path,sha256:createHash('sha256').update(bytes).digest('hex'),bytes:bytes.length});}
const data={recordedAt:new Date().toISOString(),taskId:task.id,status:'COMPLETE',dependsOn:task.dependsOn,fixtureIds:task.fixtureIds,requirementIds:task.requirementIds,checkIds:task.checkIds,states:['UI.COMPARE.EMPTY','UI.COMPARE.PARTIAL','UI.COMPARE.READY','UI.TIMELINE.EMPTY','UI.TIMELINE.KNOWN','UI.IDEA.DRAFT','UI.IDEA.RECORDED'],transitions:['T.COMPARE.CHOOSE','T.COMPARE.RELATE','T.COMPARE.REMOVE','T.COMPARE.SAVE','T.IDEA.CLOSE','T.IDEA.EDIT','T.IDEA.HELP','T.IDEA.LEAD','T.IDEA.SAVE','T.TIMELINE.SOURCE','T.TIMELINE.VIEW'],automated:{contracts:29,browserOutcomes:outcomes,basis:'Latest passing result per named check across retained runs, not one final full-suite run. The exact FIX11.TOOLS route is included for all three engines.'},native:{report:'evidence/er13/parent-native-review.md',scope:'Independent in-app developer play: Compare E2 interpretation with E3 passage; private save; known-time timeline opens exact component and restores row focus. No participant inference.'},files,boundaries:{gameplayGate:false,automaticGrading:false,sourceBodiesRewritten:false,privateIdeaAutomaticNpcDelivery:false,liveProviderCalls:0,learningOutcome:'NOT_RUN'}};
await writeFile('evidence/er13/task17-acceptance.json',JSON.stringify(data,null,2)+'\n');
console.log(JSON.stringify({task:data.taskId,status:data.status,states:data.states.length,transitions:data.transitions.length,browserChecks:outcomes.length}));
