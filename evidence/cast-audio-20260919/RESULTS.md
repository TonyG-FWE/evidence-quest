# Selected cast integration — September 19, 2026

## Current repair checkpoint — independent exact clips

The repaired catalogue contains **6,167 entries / 5,591 independent PCM WAV clips**, totaling **1,084,832,556 clip bytes**, with all eight selected voices. **85 original MP3 recordings and 85 decoded PCM masters** remain deterministic source inputs; compiled delivery selects the independent clips and manifest. The submitted originals remain preserved. The changed entry count removes extracted CSS/internal-error strings and incorporates exact current excerpts; authored source text was not shortened.

Final source manifest SHA-256: `d5e493c3168c7a3929c5ad235d5740b5eb081a2fc401806731cee727b7fd6e0b`. After all shared sources were frozen, the four provider-free preparation, binding, derivation and verification commands passed under **Node v24.21.0**: **6,167/6,167 exact bindings, zero missing recordings and zero changed recording bindings**. Verification checks current source hashes, approved speakers, apostrophe-preserving recorded words, receipt/master hashes and exact PCM sample slices. Its separate runtime scope checks compiled clip bytes/bounds; source and runtime manifest hashes must match. [Exact commands and final receipt](final-binding-refresh.json), [derivation](independent-clips.json), [semantic bindings](semantic-repair.json).

Eleven submitted word bindings changed, including **ill / I'll**, **well / we'll** and **we're / were**. Nine were repaired from existing recordings. Missing/current Pip, Grandma, Mara and narrator text, including We're/we're, required **four bounded `s2.1-pro-free` batches**. Four preceding network failures and subsequent successful requests remain in the separate cast ledger. Automatic review initially confused this job with halted TASK11.19; it accepted the verified later Fish authorization and distinct model/ledger. No OpenAI request, paid fallback, TASK11.19 retry or Tripo operation occurred. [Submitted comparison](repair-from-submitted.json), [finite plan](generation-plan.json), [ledger](attempts.jsonl).

Playback now runs complete independent files from zero to their natural end, with no batch seeking or timed audio cutoff. Loading deadlines, cancellation and ownership guards prevent failed/retired playback from advancing a telling. Dynamic explicit readings use bounded, expiring and revocable opaque HTTP URLs. Media supports single byte ranges, HEAD and unsatisfiable-range responses. An explicit performer can change the reading voice, while supplied canonical source/edition/occurrence metadata must still validate.

**Measured checks:** ten focused audio/source/range/ownership contracts passed under Node v24.21.0; the later continuous-reading change also passed its three scoped source/playback checks, including canonical-source rejection while retaining the actual performer. A native harness passes **27/27**: eight selected clips plus a synthetic dynamic-service response through real HTTP/native playback in each of Chromium, Firefox and WebKit. This completed report binds manifest `7a54916cf630358147d04a752c4b003dc0ad9ca8527e82dfce7a95109ae8741f` and records exact controller/source hashes. The dynamic response is a fixture; no provider was called by qualification. Restricted-process Firefox startup and WebKit codec failures remain recorded; the unchanged harness/clips passed in the authorized normal process context. [Native results](native-independent-clips.json), [earlier native report](native-independent-clips-prefinal.json), [restricted result](native-independent-clips-sandbox.json).

The compiled **R2 definition-narrator run passes 18/18 checks**, with no skips or unexpected failures. It exercises all eight saved voices and the actual meaning-button playback/Stop control in Chromium, Firefox and WebKit at DPR1/2, plus local-catalogue cancellation and HTTP503 checks in each browser. The latter use explicitly synthetic network faults. The all-voice samples use native media from the compiled server, not the game's source-routing UI; the meaning-button cases use the actual controller. No provider was called. This run used the R2 runtime with manifest `983141f1aabcdf3791ee485a986cab9d18c7cf5fe530c835f6baa42f130fec7b`. [R2 browser results](../demo-release-20260919/audio-fixture-repair-r2.json).

Both native runs precede the final `d5e493…` source binding. Independent clip/controller bytes remain unchanged, while reader ownership/source metadata and renderer recovery changed afterward. **These results do not qualify the successor compiled candidate or its complete reading/playthrough controls.** Successor source/runtime binding, integrated reader and dynamic-own-text cancellation checks, full-game qualification, and Tony's listening/experience acceptance remain open in the root run. Native decoding/controller checks do not establish spoken-word comprehension, acoustic voice acceptance or Tony's approval. Historical-edition audio coverage is not newly claimed.

Provider-free regeneration after an intentional source change: `prepare-cast-audio.mjs` → `repair-cast-bindings.mjs --write` → `derive-cast-clips.mjs` → `verify-demo-audio.mjs`. Missing recordings fail closed. Finite generation remains separately authorized and is never invoked by build or authored play.

## Preserved predecessor — submitted batch-seek candidate

The record below describes the earlier submission. Its decode/start checks did not detect the later batch-seeking failure and do not qualify the repair above.

The final manifest binds 6,322 authored text entries to all eight approved voices, including the previously selected Hannah voice for Rina. The runtime packet has 81 MP3 recordings and 81 WAV alternates, totaling 945,445,023 bytes. Manifest SHA-256: `a8a50ea1e04c2f3612d2fc8dcf8b1f6bf595fc9dc189c93e892d589c2d6ca136`.

`verification.json` records complete file-hash and interval-bound verification. Every source MP3 was decoded through Chromium's native AudioContext. WAV alternates use PCM16 at the AudioContext sample rate. This is decoder and timing evidence, not human listening acceptance or a complete browser qualification matrix. The final native in-game recording check belongs to the root integration task; the full browser matrix was deferred at Tony's recording deadline.

Private reading keeps exact authored spans with explicit quote-speaker metadata. Onstage playback uses the actual teller for the entire telling. Meanings, words, choices and source passages share one interruptible audio owner. An error or cancellation never advances a telling. Dynamic own writing and generated feedback use truthful origins, exact text/revision bindings, explicit Listen admission and an ignored private cache. Automatic onstage playback can only reuse that cache. Authored playback never calls Fish.

Generation used only `s2.1-pro-free` and the eight approved IDs. Reservations and outcomes are in `attempts.jsonl`; no paid fallback or TASK11.19 request occurred. The first exporter incorrectly kept only the final audio delta within each stream chunk. Native decoding exposed the truncated recordings before acceptance. The corrected exporter appends every audio delta in wire order while retaining each chunk's final word alignment. Corrected recordings and word intervals were regenerated and verified. The 161 rejected or unreferenced artifacts are preserved in ignored `.cache/cast-audio-rejected-stream-v1`, outside the runtime packet.

Focused contract checks passed 4/4: exact source reconstruction and speakers; explicit consent, revision and private cache isolation; generated-feedback provenance, pre-abort and loopback admission. Client and server TypeScript checks passed. No browser/device voice substitution is used.

Reproduction: `scripts/prepare-cast-audio.mjs` prepares source-bound entries; `scripts/generate-cast-audio.mjs --reuse-only` rebinds valid existing receipts without provider requests; `scripts/cast-audio-wav.mjs` rebuilds/verifies local alternates; `scripts/verify-demo-audio.mjs` enforces the complete runtime packet. New generation is a separate explicitly authorized command and is never invoked by build or authored play.

A native welcome check caught one further catalogue omission: the extractor treated natural language containing 'return ' as code. The filter was corrected and five bounded free requests added the missing Jo welcome and related visible instructions. The runtime audio and compressed manifest variants were copied into the running personal build, and both source and delivery packets passed verification. A regression contract requires all four exact enriched Jo welcome paragraphs.
