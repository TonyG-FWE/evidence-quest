# Test artifact retention

## Download distribution — September 26, 2026

The distribution cleanup removes 2,092 files / 105,225,703 bytes from Git tracking only. The exact [inventory](../evidence/distribution-20260926/removed-files.json) includes hashes and reasons: 1,960 recordings not selected by the current catalogue and 132 historical outputs without retained textual references. Their local bytes remain available. The original imported Python cache stays tracked because `evidence/design-import.json` explicitly lists it. Required source masters, selected clips, fixtures, source authority and durable ledgers remain.

Ignored audio is added in future only as an explicit reviewed catalogue selection; existing selected recordings remain tracked. Portable archives and browser captures stay under ignored `output/` and `.tmp/`. PR workflow artifacts are retained for 14 days; published archives belong in GitHub Releases, never the Git tree.

## Lean development follow-up — September 26, 2026

Tony subsequently requested keeping only materials useful for the game and continued development, and explicitly declined all local demo recordings because his own recording is on YouTube. This supersedes the recording-retention instruction in the earlier cleanup below. The local fallback player service was stopped; the game services were retained. Demo/rehearsal videos, generated players and obsolete captures were removed; the public authored voices and microphone implementation remain intact. Earlier playback evidence is a historical result, not a claim that its deleted video is still available.

The follow-up removed **47,958 untracked files / 18,567,775,279 bytes (18.57 GB)**: old source/compiled snapshots, regenerated experiment/test caches, npm download cache, unselected review derivatives, obsolete screenshots/pages, declined recordings and 1,844 untracked audio clips excluded from the current source/runtime selection. No tracked file was selected for deletion. Exact paths and dispositions are recorded under `evidence/lean-development-20260926/`; the [summary](../evidence/lean-development-20260926/summary.json) distinguishes logical deletion from measured disk changes.

All **23,305 protected files** matched their pre-cleanup hashes before the intentional documentation/receipt updates. Source, editable/original art, all 143 supplied models, current voice recordings, provider history, earlier modifications, Git history, tools and installed dependencies remain. Rebuilding passed; compiled bytes did not change. All 251 contracts, 23 authored checks, runtime/original-asset checks and fresh compiled browser startup passed with zero provider requests. A preservation check taken during validation observed the expected generated content-validation update; the earlier bytes were restored and the final preservation check passed. Broad browser/performance qualification remains unsuperseded.

Use [the development handoff](game-review/DEVELOPMENT-HANDOFF.md) for retained original locations, local-only assets, build commands and remaining work. The playable GitHub packet and the full local editable-asset collection are distinct; do not delete local originals merely because a demo can run without all of them.

## September 26, 2026 workspace cleanup

Tony authorized an Evidence Quest-only cleanup. Two inventoried passes removed **463,610 untracked disposable files / 140,359,089,025 logical bytes (130.72 GiB)**: 48 abandoned demo-public staging copies, 289 verified Playwright trace archives, 203 per-test videos, abandoned Playwright worker captures, old failure screenshots, three completed isolated package fixtures and the superseded compiled snapshot under `output/submitted-before-repair-20260919/dist`. The historical source snapshot remains. A final housekeeping pass removed completed server staging/backup copies and the temporary fixture created by this verification; its separate receipt avoids confusing newly generated temporary bytes with the original cleanup.

The [cleanup summary](../evidence/disk-cleanup-20260926/summary.json) records logical removals separately from measured free-space change. Exact per-file deletion journals and reviewed inventories remain locally in verified gzip archives listed by [inventory-archives.json](../evidence/disk-cleanup-20260926/inventory-archives.json). Their decompressed hashes are recorded. Every target was resolved inside this workspace, checked against tracked/protected files, and rechecked for identity, size and modification time before deletion. Links and changed/locked files were excluded; the completed passes had no deletion skips.

Small result summaries, failure descriptions, original source/art/audio, all 143 supplied original models, provider accounting, the original live audio/video capture, both packaged Nerdy fallback recordings and the latest fallback review frames remain. All ten pre-existing tracked modifications were preserved byte-for-byte; the content-validation output changed during rebuilding was retained separately before restoring the earlier bytes. Historical references to deleted raw traces, screenshots, temporary directories or the superseded compiled snapshot are provenance, not currently inspectable artifacts. Their prior pass/fail outcomes and open qualification gaps remain unchanged.

Demo public staging now uses an awaited ownership scope in both the client build and release verifier. Success, partial-copy failure and consumer failure release only that run's directory, after the consuming operation finishes. Release verification waits for child `close` before cleanup. Four focused lifecycle tests pass, including another run's directory remaining intact and a child reading assets before exit. Run them with `node --test checks/demo-public-cleanup.test.mjs`. Browser failure trace capture remains enabled; interrupted processes can still leave artifacts, which require a later ownership check before cleanup.

The provider-disabled demo rebuild, runtime/original asset checks, exact compiled-package check and isolated package verification passed. Compiled outputs are byte-identical to the prior candidate. Fresh Chromium startup and the preserved 12:02 fallback's native video/audio playback passed; the recorded-feedback chapter opens. An initial smoke-check selector failed to click the welcome button; its failure is retained alongside the corrected passing check. This cleanup does not repeat or supersede the broader gameplay/performance matrix. Git history, dependencies and the running game at port 4496 remain intact. No provider request or TASK11.19 resume occurred.

On September 16, Tony requested committing and pushing everything required for the game, with unnecessary testing records removed to free space. Source, runtime assets, original design files, test programs and setup instructions remain in the repository.

Generated browser recordings, trace archives, screenshots, raw browser output and duplicate source/build snapshots are excluded from future commits. The approved cleanup removed 299 verified Playwright trace archives and per-test video recordings, freeing 6,669,862,038 bytes (about 6.7 GB). Trace archives were identified by their actual trace entries; videos were checked for their container header. The [removed-file inventory](../evidence/repository-checkpoint-20260916/removed-playwright-recordings.json) records their paths and sizes.

Automatic approval review rejected the broader removal of historical snapshots. Those snapshots and two incomplete recording archives remain local and excluded from the game commit. No source history or original artwork was deleted. This narrower cleanup does not require Git LFS and makes no account or billing changes.

Small verification summaries are retained to preserve the actual results, initial failures and open qualification gaps. Their historical links and hashes can refer to raw artifacts no longer available in the repository. A retained result is a dated observation, not a claim that the deleted raw capture can still be inspected. Historical archive-reconstruction scripts may need those captures; normal build and test commands do not.

The following records have active dependencies and remain available:

- `evidence/group-6-review-20260915/group-5-migration-inputs.json` and `evidence/group-7-review-20260915/group-6-migration-inputs.json`: authentic saved-game fixtures consumed by current contracts and browser tests.
- `evidence/er13/production-art-exports.json` and `evidence/er13/art-runtime-inventory.json`: verified artwork cache and original-asset integrity checks.
- `evidence/group-7-finale-20260915/content-manifest.json`: generated content manifest and its current build output directory.
- Original import receipts and the `parent-import-history` / `import-history` folders: preserved authoritative source bytes.
- `evidence/er13/live-evaluation/`: the persisted provider-attempt history, including the unchanged halt at exactly 1/75. Cleanup must never reset that ledger.

The September 19 demo package adds a finite source boundary for the personal review profile. `assets/demo/manifest.json` declares every packaged file, its SHA-256, size, source path and exact hydration targets. The packet contains 151 files (585,710,767 bytes): 128 review runtime files plus their manifest, nine personal landscape derivatives, two provenance receipts, two path metadata files, five loader-verified original GLBs, the supplied-original inventory and three texture decoder files. This is a byte-identical selection, not a regenerated or quality-reduced copy. The remaining supplied originals are preserved locally; the inventory records all 143 originals without implying every original is needed by this build.

`npm run build:demo` hydrates the declared files into `.cache/final-demo-review/assets`, the selected `output/personal-landscape-20260918` and original-source paths, the two required evidence metadata files, and `public/garden-assets/basis`. It verifies existing targets and refuses to overwrite a mismatch. The clean-checkout packet covers the direct `path-support.json` runtime import and both personal asset loaders' original-hash checks. These are real build dependencies under `output/` and `evidence/`; the earlier claim that no game build reads those paths no longer applies. The normal production approval gate is unchanged.

`public/audio/cast/manifest.json` and its declared MP3/WAV files are tracked authored playback assets. Compact authored-generation records under `evidence/cast-audio-20260919/` and the durable `evidence/demo-ai-20260919/openai-attempts.jsonl` ledger retain their separate provenance. The prior `evidence/voice-ai-demo-20260918/openai-attempts.jsonl` remains part of the total recording allowance. Never reset provider accounting as a cleanup step. Generated audio for player-written text, request caches and credentials stay private under ignored `.cache/cast-audio-dynamic/` and `.env.server.local`.

New `.gitignore` rules exclude the generated `output/` tree, hydrated `public/garden-assets/`, recording and trace archives, and named duplicate candidate source/build trees. They do not delete or untrack originals, existing history, active saves or existing receipts. Do not stage all ignored files with a broad force-add. Stage only the finite demo manifest's files, its documentation/verification receipt, the packaging scripts, the declared authored-audio files and separately reviewed compact evidence. `.gitattributes` disables line-ending conversion for `assets/demo/**` and `public/audio/cast/**` so a Windows checkout preserves their hashes.

Historical full-original verification, source reconstruction and playback-recording helpers can still require local files outside the finite demo packet. Their old paths and hashes remain truthful provenance; a clean checkout is not claimed to reproduce every historical capture. The playable package, its current build, current tests and each dated evidence claim have separate boundaries. See [the current README](../README.md) for the reproducible local demo commands.
