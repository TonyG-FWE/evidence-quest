# Production candidate qualification

Current candidate: App-BzEahbjQ.js with App-CvfCOyJg.css. The final affected three-engine suite passed 63/63 checks; 39/39 deterministic contracts and 5/5 codec/attempt-ledger checks also pass. The isolated 15-case density/format matrix completed 13 pass/2 fail in 7.1 minutes. Only WebKit DPR2 active cadence remains outside the unchanged target: 51 ms WebP and 52 ms PNG. Earlier failed measurements remain evidence, and neither source delivery nor native screenshots alone establish performance acceptance.

## Sources and rendering

All 51 accepted manifest PNGs plus Stage remain unchanged: 52 sources totaling 71,963,514 bytes. The 139 canonical runtime bindings use 267 unique lossless WebP/PNG pairs. Every pair was decoded and compared byte for byte, including fractional alpha and transparent RGB. Crops, anchors and source detail are retained; no derivative enlarges a source. Cutouts have 4/8 transparent padding pixels. [Exact verification](../evidence/er13/production-art-verification.json).

The four directions, real gait, reach/carry, crew delivery poses, Loop states, kit and physical sources are connected to their original owners and endpoints. The carried kit is closed and hand sized; the open inspection case stays at a workstation. Paper terrain clips to the original polygons; planting, seed, roots, flower and light retain their specified timing. The frozen E2 photo remains curled after E3 is secured, and the complete canonical E3 text stays native and readable. All 28 original clip implementations have an explicit [motion review](../evidence/er13/motion-review.json).

Density follows actual display geometry. Home's cover backdrop now uses the same exact PNG contingency as other consumers. Native images and Canvas share an8MiB immutable encoded-response pool, including in-flight ownership, release and explicit failure retry. This avoids duplicated response bodies even when forced-PNG routing disables the browser's normal HTTP cache. Local object URLs are permitted only by the image CSP; other resource policies stay scoped to the same origin.

The decoded Canvas pool reserves at most48MiB estimated RGBA. Exact-device-pixel static surfaces have a separate24MiB ceiling. Their source dependencies stay owned, and decode/retry, geometry or committed object changes invalidate the relevant layer. Incomplete artwork cannot become a finished cached layer. Physical operations draw live. A stationary Stage rehearsal can repaint just the changing projection rectangle while retaining its unchanged room and actors; input, cue/case commit, image change or resize forces the complete frame. The Store's original single-state publication behavior remains intact. [Measured correction history](../evidence/er13/rendering-corrections.md).

## Limits and measurement method

The parent's explicit [section10 allocation](design/evidence-quest-design-v3/13-EXPERIENCE-AND-LITERACY-CORRECTION.md), SHA256 c8f22de816b7220de46a74357c1905de8259ceb9e83bc465514588a732cc291f, retains WebP4MiB initial/3MiB later room and allocates identical-detail PNG5MiB initial/4.25MiB later room. This was an engineering decision under existing authorization, not a claim that the user chose these numbers. Input remains strictly below100ms; active RAF p95 must be at most33.34ms. Current/global conservative memory remains96/192MiB.

Original all-variant group allocations are preserved in the unchanged Item10 manifest. All base WebP groups fit; high Courtyard/shared/puppet and several PNG inventory totals do not fit those original allocations. The exact failed totals remain in the export verification as historical comparisons and inventory warnings. They were not relabeled as original passes; section10 explicitly uses actual format-specific visits and lifetime accounting for current acceptance.

Measurements use this Windows host, loopback,1440x1000 CSS pixels and DPR1/2 emulation. The test runs a fresh four-room visit sequence and a separately labeled READY fixture for the two-Canvas rehearsal. It requires visible changed story pixels and changed committed description during the same active two-second interval. RAF spacing is a scheduling proxy, not physical-display FPS. Raw event-to-pixel input includes readback time without subtraction. Body-byte totals exclude HTTP headers. Conservative memory includes decoded reservations, exact offscreen surfaces, native image/SVG consumers, encoded responses plus an equal transfer-copy allowance, live backing/compositor buffers and explicit reserves. It is not measured total browser/GPU residency.

| Browser | DPR | Format | Initial bytes | Input ms | Active p95 ms | Verdict |
|---|---:|---|---:|---:|---:|---|
| chromium | 1 | PNG | 4174889 | 11.6 | 16.7 | PASS |
| chromium | 1 | WebP | 2925390 | 20.9 | 16.7 | PASS |
| chromium | 2 | PNG | 4700187 | 18.9 | 16.7 | PASS |
| chromium | 2 | WebP | 3587100 | 8.8 | 16.8 | PASS |
| firefox | 1 | PNG | 4174889 | 6 | 18.22 | PASS |
| firefox | 1 | WebP | 2925422 | 8 | 18.22 | PASS |
| firefox | 2 | PNG | 4700187 | 15 | 18.28 | PASS |
| firefox | 2 | WebP | 3587132 | 11 | 18.22 | PASS |
| webkit | 1 | PNG | 4174889 | 10 | 17 | PASS |
| webkit | 1 | WebP | 2925390 | 9 | 17 | PASS |
| webkit | 2 | PNG | 4700187 | 17 | 52 | FAIL: cadence |
| webkit | 2 | WebP | 3587100 | 15 | 51 | FAIL: cadence |

All12 records identify App-BzEahbjQ.js. Independent arithmetic checks of every recorded criterion confirm the two cadence misses are the only failures. Maximum current memory is81,671,594bytes, global162,217,072bytes; maximum raw input20.9ms and first-usable1,835ms. All three actual two-format art-failure/retry cases pass. [Exact reports, criteria and bundle hashes](../evidence/er13/production-qualification.json).

## Behavior and native review

The full Dv0 baseline completed192pass/4fail/2plannedskips. Successive bounded fixes address the original focus, compact visibility and actual module-retry failures. The final63 checks cover all five successful plans in every engine, narrator pauses, explicit foreground events, source fidelity, rail focus, all-Canvas-denied play, cold and failed artwork, actual blocked/HTTP503 application retry with saved-byte preservation, native PNG Home, resizing and complete320x568/640x360 Largest/Roomier consequences. The nine development-only coaching checks pass with a declared synthetic transport and real timeout. These reports form a latest-named-behavior union, not one final198-case rerun.

The complete96-state/121-transition/15-route [crosswalk](../evidence/er13/state-transition-route-crosswalk.json) retains each original rule and maps it to named executed behavior checks. It is a review index, not an instrumented hit counter or96 separate screenshot assertions. The39 contracts include all65 story orders, five successful orders,36 cue-race combinations and ownership/retry/budget boundaries.

Independent parent review covers the actual connected case, readable story/kit/Toast, source/reading/reasoning tools and both complete compact captions. On the final candidate, ordinary Continue at the parent's original 4176 origin retained the completed FBPL case, docked Loop and seated kit. Move to → Replay premiere physically approached the pad and completed the illustrated ending; the exact earned reading passage and optional support remained available. The builder separately reopened the saved Stage at 4175. Both reviews preserved their original saves and remain distinct from automated fixtures. [Final native review](design/evidence-quest-design-v3/13-experience-correction/PARENT-NATIVE-REVIEW.md), [builder Stage](../evidence/er13/native-final-bze-stage.png), [preview ownership and restart](../evidence/er13/final-preview-restart.json).

Actual app-window foreground/background remains NOT_RUN: the earlier display request was queued while its task was hidden and did not prove a physical event. Touch is emulated; physical mobile devices and all-device screen-reader coverage remain NOT_RUN. No participant appeal, learning improvement or all-device durability is inferred.

## Independent live-evaluation blocker

TASK11.19 is authorized and configured, but its one actual provider attempt returned HTTP429 insufficient_quota / credit_balance_exhausted after2,291ms. No model output was returned. The durable ledger records1/75 attempts used,74 remaining, with no automatic retry or cap reset. Funding and an explicit coordinated cap-preserving resume are needed. Authored coaching remains the player default and child-live is disabled. [Actual attempt](../evidence/er13/live-evaluation/attempt-001.json).

Optional ART08 foley is omitted; ART09 effects remain partial. Deployment, public publication, release, submission, child-live activation and participant/learning evaluation remain outside this checkpoint.
