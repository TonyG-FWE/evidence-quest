# Staged bridge and complete chapter amendment · 16 September 2026

This continues TASK11.05/.06/.10–.16/.20/.21 in the existing `codex/first-connected` checkout at `7cc938a10bb52226a7a13493b64828eccf9c53d2`. It supplements the imported implementation plan and its existing CHECK11/FIX11 bindings; it does not replace their sequence or declare complete chapter acceptance. The pre-change local files are preserved in `evidence/staged-bridge-20260916/baseline.json` and its referenced copies. Earlier artwork, rejected rigs, source approvals and verification records remain intact.

## Bridge correction

The serialized river construction record has version 1, a selected first section, six installed-post facts, and west/center/east attachment facts for each of two side ropes. Section support is derived from those facts and actual section/post geometry. Rendering, navigation, save validation and collapse use the same definitions. Both unsecured decks remain attemptable; no required failure or reading gate was added.

A release ends the placement gesture and returns to movement. Escape restores the last committed placement. Clicking a deck walks; dragging it beyond the small movement threshold starts manipulation. Posts and rope endpoints snap to their actual targets with previews. The first secured section becomes a walkable working surface before the second is placed. A loose second section can collapse while the first, its four posts and its attached rope spans remain secure. Pip retreats to a stable surface and the same materials can be recovered.

The development presentation shows six physical posts, loose ends, slack rope movement, wrapping, tightening and persistent knots. Pip's approved carrying pose faces the work. Two separately authored full-body post/tying clips are review-only; they are not active gameplay animations or approved final artwork. New structural geometry does not claim finished character contact.

Legacy saves are archived before construction migration. Completed crossings remain complete. Prior near-only and far-only anchors retain their corresponding supported sections, including swapped section identities. The far-only case retains its center posts and can recover the missing near span without discarding existing work. Held gestures and animation time are not saved.

## Story reconciliation and chapter work

The local `codex/deeper-garden-stories` source at `9ea8234665a4409aa6817f53f469fe4f2e689dd5` was snapshotted and reconciled against the common base and current gameplay changes. `evidence/staged-bridge-20260916/story-integration/` retains the three-way inputs, resolved source identities and checks. Its remote publication was not verified; local provenance is the implementation authority here.

Fresh adventures use `literary-20260916`. Existing adventures retain their original narrative edition, writing, choices, source exposure, physical history and endings. The staged maintenance note has independent `GA.SRC.SECTIONS.STAGED20260916.` references in either edition. Reading exposure, observed events and possession remain separate.

Connected gameplay retains boat steering and recovery, actual seed delivery or carrying, soil preparation/planting, earlier-event wing repair, roof placement, dough preparation/division, optional unshaped batch recovery, workshop writing/rehearsal, invitations, gathering, memory placement and studio presentation. Additional presentation changes include actual committed dough portions, hand-following tape, and persistent optional workshop pictures with bounded texture ownership.

## Artwork, performance and delivery boundary

Pip and Grandma R2-feet source approval and the village layout/reference approvals remain valid. No renewed approval is requested. The boat, rooted flower, local travel gait, painted environment and new construction actions remain behind matching Form approval. Local remaining-cast studies are explicitly unfinished references, not production character models. Loop has a local review candidate; Jo remains an unfinished form study. Five painted appearance references for Mara, Rina, Sol, the boy and the boat operator are proposed for reference approval. These are images, not integrated 3D models. Remaining cast production is a chapter requirement, not an acceptance waiver.

The updated environment review preserves the previous kit and adds a local natural perimeter around the approved irregular banks. It does not change navigation or install new scenery into the playable build. The chapter export gate binds individually accepted models and the environment's reviewed source/dependency hashes; approval of one item does not approve another. No pending approval receipt has been written.

The requested Build 3D Game Rooms skill requires: “Record explicit human Form approval. Stop before progressive export when Form fails.” Pending artwork is isolated in the consolidated chapter review; only existing approved runtime sources may be exported by the normal build.

Actual WebKit DPR2 cadence and 2D studio transfer failures remain open until measured passing corrections are recorded. Timing diagnostics do not qualify altered diagnostic views. DPR, geometry, textures, antialiasing, shadows and published thresholds must be retained. Detailed results and final command outcomes will be recorded in this amendment's dated verification evidence after qualification.

No new Tripo request, reroll, paid rig/animation or subscription was made. Recorded spending remains 290 credits within the existing 400/1,600/500 allocations and 2,500 total ceiling. TASK11.19 remains halted at exactly 1/75. Authored play and verification require no live AI.

The six-condition staged pointer route passes in retained evidence; this proves those inputs and consequences, not performance, finished Form, accessibility on physical assistive devices, enjoyment or learning gains. Full chapter qualification and Tony's play review remain separate acceptance requirements.

The first full functional runs use a frozen production bundle. Later bounded corrections (partial-rope migration, collapsed/swapped section recovery, stable overlapping support, reduced-motion model-arrival redraw, geometric canopy visibility and staged-note vocabulary context) are separately tested before final rebuild. Reports must distinguish that full-run bundle from the final affected-scope checks. The retained workstation's five equal tile arrangements and the village's physical histories are separate coverage categories.

Before final isolated timing, agent-created animated review tabs were closed and the user's adventure was paused through its native Pause control. Inspection found those background Codex tabs still reported visible and rendered continuously. Earlier timing failures remain recorded with that limitation; a later quiet run must establish any performance resolution. The user's save was not reset.

## Qualification results · 17 September 2026

All eight required commands were executed. Install, content, type/schema/copy, server, 192 contracts, 23 authored coach checks and the final production build pass. The final build's first restricted-filesystem attempt failed in Vite; the authorized normal-filesystem rerun passed. The required full Chromium browser command retains its 151 passes and three failures. The other five functional conditions retain 361 passes, ten failures and four explicit non-Chromium CDP-touch skips. These broad reports used the preserved earlier production bundle; final bounded corrections have separately identified evidence, not a retrospective all-green full-suite claim.

Across those broad reports, all nine fresh narratives and separately all nine saved narrative replays pass in each of six browser/DPR conditions (54 each). All five original workstation arrangements pass in each condition (30). The four ordinary village histories have 18 initial passes plus six passes after correcting the FBPL Places-menu fixture. The automatic planting growth does not create an ordinary BPFL village route; its interrupted-state compatibility is separately tested. Corrected archived migration passes 6/6. Oldest prototype BOATS exposure keys retain their pre-existing archive/retirement behavior; this rewrite did not introduce that rule.

The final affected build reports 42/42 input/state checks passing, covering staged construction, roof/dough manipulation, both narrative editions' reading support, delayed approved-model arrival, gathering and reading focus. There are also 35 passing character, resource and export-gate checks. All 267 PNG derivatives preserve exact decoded RGBA, saving 1,857,326 bytes; original sources and all WebP bytes remain unchanged.

**That is not complete visual qualification.** WebKit DPR2's roof, dough and completed-bridge screenshots are blank despite passing interaction assertions. The reduced-motion model-arrival screenshot displays the world. Isolated same-frame diagnostics observe valid opaque village pixels in a healthy context while page/canvas captures can remain blank. A separately labeled preserved-buffer comparison also fails to restore capture; no production context option was changed. This distinguishes successful scene drawing from unresolved presentation/capture evidence, and does not establish native WebKit visual acceptance.

The isolated actual-game matrix finishes 17 passed / one failed with all other project previews closed, one browser, actual DPR and unchanged detail/limits. WebKit DPR2 measures 39 ms bridge / 43 ms overview p95 against 33.34 ms. Chromium and Firefox DPR1/2 and WebKit DPR1 meet the cadence limit. All six measured openings meet five seconds (4,250–4,567 ms under the established network profile); native-key-to-position/next-frame observations are 20.9–66 ms, with their explicit non-hardware scope. Initial/core/location transfer and conservative scene/cache accounting meet their limits. These results do not waive the WebKit cadence or presentation gap.

The original-studio format matrix finishes nine passed / six failed: all six DPR2 WebP/PNG room-transfer cases fail. The final Chromium DPR2 Courtyard loads 4,079,308 WebP art bytes against 3,145,728, or 5,653,933 PNG bytes against the existing Section10 allowance of 4,456,448. Its later Stage visit also exceeds the applicable room limits. Initial transfer remains within the existing allowances. No limits, texture detail, DPR or geometry were reduced. A four-layer full-detail Courtyard composite was tested locally and rejected as larger; sources and production remain unchanged.

Exact reports, scopes, hashes and retained failed runs are in `evidence/staged-bridge-20260916/verification.json`. Native physical-device/assistive-technology checks, human microphone/acoustic quality, enjoyment and learning improvement are NOT_RUN. Installed Windows WebKit lacks the required native audio interfaces; its unavailable path is tested separately from Chromium/Firefox's synthetic microphone capture.

TASK11.20/.21 and complete-chapter acceptance remain open. The next artwork dependency is explicit per-item Form approval of the review-ready props, environment, new gaits/actions and Loop, plus reference approval of the five proposed cast images before finished cast production. Existing character-source, layout and original-reference approvals stand. The unfinished cast/Jo studies are not submitted as finished models. Tripo remains 290 credits; TASK11.19 remains halted at 1/75. Owner play review is still required.

### Later first-play-layout correction

The rebuilt checkpoint delays initial scene creation until the opening reader has closed. WebKit DPR2 now presents actual terrain and character pixels during a fresh adventure, movement and a second adventure. This is a partial correction: later reader return and ordinary viewport resizing still lose presentation. Fixed-size inert toggling does not reproduce the fault; actual backing-size changes do. Waiting for acknowledged render dimensions and settled frames does not restore the picture. Neither an identity-transform layer nor reinserting the same canvas corrected it, and neither diagnostic was adopted. The default renderer, actual DPR and visual detail remain unchanged.

`browser-tests/garden-world-presentation.spec.ts` now asserts actual captured scene pixels and retained canvas identity, so state-only success cannot hide this presentation failure. The complete diagnostic chain and rejected probes are retained in `evidence/staged-bridge-20260916/world-presentation-lifecycle-disposition.md`. The full production rebuild passes in `required/build-lifecycle.log`; its exact source and distribution are bound by `lifecycle-build-binding.json`. Later affected-path and isolated performance runs must be attributed to this binding; the earlier 42/42 and 17/18 results above retain their original scope.

The rebuilt affected scope completes **41 passed / one failed**. Every staged-bridge browser/DPR case passes. WebKit DPR2's gathering case timed out after15seconds waiting for Sol; its failure snapshot already displayed Sol's arrival. A separate unchanged one-browser rerun passes. This is not a clean42-case rerun. A first launcher invocation used the missing system browser cache and failed before any application execution; the normal project wrapper/cache invocation and that configuration failure are both retained.

The rebuilt screenshot regression completes **one Chromium DPR2 pass / one WebKit DPR2 failure**, retaining the later-reader presentation defect. Isolated performance completes **17 passed / one failed**: latest WebKit DPR2 bridge/overview p95 is **61/60ms**, versus33.34ms. The preceding39/43ms result does not describe this latest run. Chromium DPR1/2 is16.8ms, Firefox18.36/18.38ms, and WebKit DPR1 is31/32ms. Measured opening spans4,237–4,475ms and native-position/next-frame input observations19.7–53ms. All measured garden transfer and conservative resource limits pass. Native display latency and total driver/browser memory are not established by those measurements. The unchanged studio source retains its separate9/6 failing transfer matrix.

`lifecycle-summary.json` records actual metrics, pixels, raw counts and unchanged bound source/distribution identities. `verification.json` attaches these later executions as separate supplements. The owned4323 preview has been restarted in authored mode without resetting browser storage. The final demonstration uses inspected Chromium DPR2 captures from this rebuild; older bridge and boat videos remain explicitly dated. Human Form, complete-chapter play acceptance and the unresolved WebKit/studio limits remain open.

### Final background-rendering check

When reopening the playable preview, an animated review tab was unexpectedly present. Only the authentication tab had been observed before the rebuilt timing run; the review tab's arrival time is unknown. The61/60ms result is preserved, but a continuously quiet background cannot be claimed for it. A separately recorded three-case WebKit DPR2 run with the review's native Pause active gives two passes and one failure at42/43ms. Source inspection confirms that Pause freezes model motion while retaining the viewer's render loop, so that run is not treated as renderer-free either.

The final unchanged three-case run uses only the authentication tab and the static comparison page: observed zero canvases, zero iframes, both videos paused and no autoplay. It gives **two passes / one failure at41/42ms** against33.34ms. This is a bounded WebKit DPR2 recheck, not a new six-condition qualification. All190 source/distribution files in the lifecycle binding remain unchanged. `final-measurement-context.json` retains both new raw reports and their conditions; `verification.json` attaches each separately. No quality, DPR, limit or assertion was relaxed, and no result is averaged or substituted to claim acceptance.
