# Integrated river-to-bakery amendment · 16 September 2026

This continues TASK11.20/.21 and their existing dependencies on base `7cc938a10bb52226a7a13493b64828eccf9c53d2`, in the existing dirty `codex/first-connected` checkout. The imported implementation plan and [retained task/check/fixture bindings](../../evidence/hands-on-20260916/task-bindings.json) remain the authority. This intermediate checkpoint does not close the complete-chapter commitment, ER13.06–08 or owner play acceptance.

Tony explicitly approved Pip and Grandma's corrected R2-feet sources. [The approval receipt](../../evidence/integrated-checkpoint-20260916/character-form-approval.json) binds Pip `61006b3597984f6e8ceca9d08a0da39cd1e5ff14b407da439672262224024905` and Grandma `173164a1639606f0565aca5e8caa22d177d365d6dfb5f1c0ea360f58eeff1a67`. Those model files, rejected candidates and original references remain unchanged. Dated verification runs are retained; prescribed tests also refresh their normal mutable latest reports under `evidence/er13`. This checkpoint records separate run files. Layout/reference approval is carried forward. The prior source/adapter/test baseline is archived in [baseline.json](../../evidence/integrated-checkpoint-20260916/baseline.json).

## Implemented work and the remaining art boundary

- TASK11.07/.16: the two approved characters use the shared actor adapter in the real village and later gathering scenes. Named bone and clip mappings, distance-based playback, blended transitions, terrain grounding, actual palm attachments and independent skeleton leases replace the old procedural character limbs. Rina, Sol and the other existing cast retain their current artwork.
- TASK11.05/.14: Grandma's seed-collection route is committed in `river.journeyVersion: 1`. Pip can move while she travels. Delivery belongs to the serialized store; source exposure and possession remain separate. Migration archives the prior envelope and preserves writing, choices, objects, completed work and endings. Live clip time, loaders and held gestures are not serialized.
- TASK11.14: long NPC trips no longer starve the save debounce. Intermediate travel writes occur at least once per second while input continues, and possession/consequence changes write immediately. Reload resumes Grandma's return with the same seed and one delivery record. Planting waits for her return, including the alternate route that unloads the boat and carries the seed across.
- TASK11.06/.10–.12: the existing manipulation commands remain the only physical-action path. Release commits valid sections, ropes, seed placement, soil work, roof tiles and dough operations. Pointer and native controls use the same commands. Visible carrying/handoffs now follow the approved palm sockets. Boat and flower adapters are prepared behind the remaining Form gate; their old development geometry remains in the game until approval.
- TASK11.06/.10: continuous boat targeting intersects the actual water plane. A raised bank no longer redirects a water gesture. The direct route covers loading the held seed, recovering from a rock, steering around both channel obstacles, automatic receipt and free Pip movement during Grandma's return.
- TASK11.20: hash-verified Meshopt/KTX2 runtime copies retain original geometry/skin/clip accessor values and 2048px texture dimensions. ETC1S uses quality 255; it is texture compression, not a claim of pixel-identical source textures. Separate lossless welcome WebP copies are checked against every original RGBA pixel. Sources remain intact.
- TASK11.20: location demand, adjacent encoded prefetch, active-interaction retention, bounded encoded cache, late-load disposal and explicit texture-failure recovery use the existing view restoration flow. Detailed skins do not participate in ray picking. Resource accounting includes imported geometry, animation arrays, skeleton buffers, texture mipmaps, encoded cache, render backing and conservative reserves; it is not a measurement of total browser/driver memory.
- TASK11.20: static palette colors are stored as the same linear RGB vertex values, reducing full-village draw calls from 421 to 219–220. Triangle positions, normals, shading parameters and shadow participation are retained and checked. Timed actions no longer re-render unchanged native controls for elapsed time alone; authoritative simulation, action boundaries and deliberate input remain immediate. Neither optimization resolves the WebKit DPR2 qualification failure by itself.

The review-only package contains the existing seed boat and rooted flower, a separate Pip derivative adding jog/carry-jog clips, and the local painted village kit. The four approved clips, anatomy, weights, backpack and forward feet are unchanged in the gait derivative. The review includes all sides, normal/quarter-speed motion, starting, stopping, turning, hand-to-cradle-to-soil contact, and activity/gameplay camera scale. [Consolidated review](http://127.0.0.1:4318/pilot/checkpoint-review/review.html).

These four additions are **not approved**. `export-garden-assets.mjs` will only select them after an explicit Tony approval record matches their review hashes. Until then, the new travel gait and painted kit are excluded from production, and the approved walk retains the previous open-path travel timing. Natural open-path jogging and final boat/flower visuals therefore remain a visible integration dependency. The current character sources and layout do not require another approval.

The requested [Build 3D Game Rooms skill](C:/Users/TonyGuillaro/.codex/plugins/cache/openai-curated-remote/build-3d-game-rooms/0.3.3/skills/build-3d-game-rooms/SKILL.md:51) requires: “Record explicit human Form approval. Stop before progressive export when Form fails.” Pending model approval is a real export boundary, not inferred owner acceptance.

## Performance investigation and qualification scope

The existing WebKit DPR2 failure was reproduced in the actual game. Preserved attempts include renderer/DOM/shadow/compositor diagnostics; altered diagnostic views are never qualification passes. No scene detail, actual DPR, antialiasing, shadow-map resolution or acceptance threshold was lowered.

The pinned Playwright 1.61 Windows WebKit launcher adds `--disable-accelerated-compositing`. An early probe with that flag removed measured 30/32 ms at DPR1/2 in `integrated-perf-9.json`, but expanded isolated qualification did **not** reproduce that result: accelerated DPR2 runs failed at 54–91 ms. The early probe therefore does not resolve the original failure. Final qualification uses the engines' default launch arguments, separate density projects, the actual production server, both loaded characters, and the full village overview.

Current qualification work retains 4 MiB initial transfer, 1 MiB compressed core, 3 MiB per location, 96/192 MiB decoded scene/cache, five-second opening at the established 10 Mbps/100 ms profile, 33.34 ms active-frame p95 and 100 ms input feedback. Network-profile testing streams actual bytes through a bounded loopback transport; physical internet/device measurements remain separately unperformed. Recorded failures are assertions, not merely booleans in a report.

The final frame-time matrix has **five passes and one failure**. Values below are active RAF p95 in milliseconds; the unchanged limit is 33.34 ms.

| Engine | DPR1 encounter / overview | DPR2 encounter / overview |
| --- | --- | --- |
| Chromium | 16.80 / 16.80 | 16.80 / 16.80 |
| Firefox | 20.42 / 20.24 | 20.36 / 20.32 |
| WebKit | 17 / 17 | **42 / 43 — FAIL** |

All six conditions remain under the other measured allocations: initial transfer 3,963,466–3,980,874 bytes, compressed core 648,366–665,742 bytes, and maximum accounted scene/cache 69,606,522 / 135,500,578 bytes. Each imported location remains under 3 MiB. Twelve final asset tests pass: cold opening at the 10 Mbps/100 ms profile takes 4,250–4,507 ms, and native input feedback takes 22.8–84 ms, against the original five-second/100 ms limits. Failure/retry preserves the actual save and hand contact.

A clearly labeled synthetic diagnostic, containing no game, models, shadows or React, measured 31 ms p95 merely clearing a WebGL framebuffer of the same 2560 × 994 size at DPR2. This supports a substantial Windows WebKit compositing cost; it is not proof that all remaining cost belongs to the browser, nor a game qualification pass. WebKit DPR2 remains **OPEN**. No visual detail, density or threshold was reduced to hide it.

Final bounded diagnostics also retain the failed alternatives rather than shipping them: preserving the framebuffer measured 44/52 ms encounter/overview p95; explicitly creating an opaque context measured 44/40 ms. Inspection of installed Three r186 confirmed its `alpha:false` renderer option does not make its internally created WebGL context opaque. The comparison recorded actual context attributes. The production renderer is unchanged; these diagnostic injections are not qualification passes. Typical JavaScript plus draw submission was 3–4 ms, which does not prove that all remaining frame delay is external.

## Exact verification and delivery

[The final verification record](../../evidence/integrated-checkpoint-20260916/verification.json) binds source and runtime hashes, command logs, each browser result, failed attempts and the final measurements. All eight required commands ran. Install, content, type/schema/copy checks, server build, production build, **157 contracts** and **23 authored coach checks** pass; authored checks used zero API calls. **29** source/rig/gait/attachment/disposal/resource/batching checks pass.

The required full Chromium suite finished with **147 passes and three failures**, including **all nine fresh complete story routes passing**. Its seed-coordinate/arrival fixture failure was corrected and passed in the focused report. The other two failures are the older 2D transfer failures below. That full run precedes the final water-targeting, UI-time and batching corrections; later affected-case results are recorded separately, not presented as a second all-green full run.

The five equal physical arrangements and nine saved narrative outcomes passed in **84 browser/density fixture replays**. These are distinct from the nine fresh full story runs. The initial six-condition functional matrix recorded 87 passes, four explicit CDP touch skips and five failures; its boat/persistence and native-control failures have passing focused corrections. Both direct-pointer and native boat delivery now have passing cases in all six conditions across the retained correction runs. Final Chromium DPR1 and WebKit DPR2 complete native river/bakery/rehearsal routes both pass, without diagnostic injection, force clicks or framebuffer recording. The four skipped Firefox/WebKit two-contact cases are not native touch proof.

The final review viewer passes **six** browser/density cases covering eight view angles, all five inspection modes and gait controls. These tests cannot grant Form approval. [Play the authored checkpoint](http://127.0.0.1:4323/garden), [see the before/after demonstration and uncut pointer-controlled boat delivery](http://127.0.0.1:4318/pilot/checkpoint-review/checkpoint.html), or [inspect the pending artwork package](http://127.0.0.1:4318/pilot/checkpoint-review/review.html). The owned preview is recorded in `playable-delivery.json`; existing save origins were not cleared.

TASK11.20 retains CHECK11.VISUAL/PERFORMANCE/X01/X03/H03/D02 and FIX11.ART/ART_FAILURE. TASK11.21 retains its existing SAVE/INTERRUPT/RECORDS/TOOLS/HARDENING and ACCESS/SAVE_RECOVERY/SAVE_INTERRUPTED bindings through the linked task ledger. The complete chapter, final-art integration, physical-device/assistive-technology proof, owner play acceptance and ER13.06–08 are not closed by these bounded checks.

The unchanged older 2D studio has two separate required-suite transfer failures: DPR2 lossless WebP cold transfer is 4,203,826 bytes (4 MiB limit), with Courtyard art at 3,600,426 bytes (3 MiB limit); PNG fallback cold transfer is 6,055,850 bytes and Courtyard art 5,325,020 bytes, also above that older format's existing 5/4.25 MiB allocations. Those historical allocations were not changed here. These failures keep the broader TASK11.20 qualification open; village results cannot turn them into passes.

A local lossless packing experiment retained all pixels and dimensions but increased the two sampled walking animations from 972,942 WebP bytes to at least 1,004,052 bytes. It was not integrated. Existing source images and their runtime manifest remain unchanged; no lossy substitute or raised transfer limit was used.

## Budget and authorization

Additional Tripo jobs/charges: **zero**. Recorded spend remains **290 credits** with the 400/1,600/500 allocations and 2,500-credit ceiling unchanged. No subscription, reroll, conversion, paid rigging or animation attempt. TASK11.19 stays halted at exactly **1/75**, without a provider request, retry or ledger reset. Authored operation needs no live AI. All work is local; no commit, push, deployment or publication is performed by this checkpoint.
