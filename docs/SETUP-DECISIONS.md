# TASK11.01 setup decisions

Recorded September 12, 2026. These are narrow execution findings, not a new product plan.

1. **Dedicated workspace.** The user opened `C:\Users\TonyGuillaro\OneDrive - Summit Gov Solutions\Documents\ChatGPT\Evidence quest` and required all work in that dedicated workspace. It contained only an unborn Git repository and no remote. Use that root rather than create a nested repository or write to the historical proposed visualization sibling. The original 307-file design source remains unchanged; `main` commit `6cad51f0756939e7f293593cafd6ec54ce9947a2` preserves its complete byte-identical import. Subsequent status/technical corrections are dated in the repository copy and this record.
2. **Account and author.** Existing GitHub keyring session verified as TonyG-FWE, numeric account ID 217523024, profile name Tony Guillaro. Replaced inherited test/test@example.com only in local repository configuration with the profile name and GitHub ID-based noreply address. No global Git settings or credentials changed.
3. **Runtime.** Official Node 24.21.0 Windows x64 archive, SHA-256 `158f7685b44de51f6c0df1d153526cbcd3e1bc739a8dfc607721cef75de9e541`, is installed under ignored `.tools`. It includes npm 11.19.0. `scripts/npm.ps1` selects it; a host npm wrapper failed while the installed direct npm entry point worked. Node 22 remains unchanged. A mistaken `npm exec -- node` invoked npm's cached Node package for one schema check; that result was discarded and the check rerun directly with verified Node 24.21.0. No package or global runtime selection uses that command.
4. **Security qualification of baseline pins.** Initial exact-pin installation succeeded and produced the preserved [initial npm audit](../evidence/npm-audit-initial.json). Vite 8.0.10 is affected by the Windows [alternate-path disclosure advisory](https://github.com/advisories/GHSA-fx2h-pf6j-xcff) and [UNC-path editor advisory](https://github.com/advisories/GHSA-v6wh-96g9-6wx3). Both list **8.0.16** as the first fixed Vite 8.0 release. Adopt 8.0.16, with the existing plugin-react 6.0.1 peer range satisfied. Ajv 8.17.1 is affected by a [`$data` ReDoS advisory](https://github.com/advisories/GHSA-2g4f-4pwh-qvx6); this app does not enable `$data`, but uses the first fixed release **8.18.0** for a clean checked baseline. These are the smallest published fixes in the existing major lines, verified in the npm registry. All other prescribed package versions stay fixed. No `audit fix --force` or broad upgrade was used.
5. **Imported cache evidence.** The complete source includes one historical Python bytecode cache. It is preserved inside the imported design baseline because the complete byte-identical import is explicitly required; newly created caches are ignored and excluded from staging.

Any claim of successful clean installation, compilation, content validity or runtime behavior requires the corresponding executed evidence in BUILD-STATUS.md. Final illustration, live interpretation, hosting and submission remain outside this setup correction.

## TASK11.02 content binding repair

The Item 09 `Tile.labelCt` field requires a canonical CT identifier, but Item 07 section 6.1 defines the four exact tile names only in prose and the 553-entry registry contains no corresponding label IDs. Binding that field to a description would display different words. Add `CT.TILE.LABEL.FERRY/BRIDGE/PLANT/BLOOM` to the technical copy/registry with the already-approved exact names One Boat / Joined Boats / Hill / Flower. All original 553 entries remain intact; the compiled catalog has 557 entries. This supplies references for existing words, with no new variant, clue, tile rule, source body, case/version identity or behavior. The owning Item 07 addendum records this narrow contract repair.
