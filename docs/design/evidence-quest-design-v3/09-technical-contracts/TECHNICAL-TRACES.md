# Item 09 — technical scenario traces

These are written contract walkthroughs. Isolated executable checks are separately identified in validation-report.json; none is browser/player proof. Every route permits optional reading/explanation/help and remains playable with the service unavailable.

## Baseline: all fifteen Item 06 flows

| Flow | Actual route/state/transition chain | Guard, committed response and recovery |
|---|---|---|
| F01 first visit | UI.HOME.CHECKING → T.HOME.CHECK → UI.HOME.EMPTY → T.HOME.START → UI.GUIDE.OPENING / UI.WORLD.IDLE. T.GUIDE.DISMISS retains UI.GOAL. | Confirm absent save, initialize Stage once. MODEL.TAB/Show Preview are guidance, not exit locks. ST.EXIT.CY and ST.EXIT.WK remain usable; fresh target cancels pending guidance approach. |
| F02 correct-first | ST.SOURCE.E4 → UI.SOURCE.TEXT → ACC.VENUE / UI.NAV.MAP → T.NAV.GO through ST.EXIT.WK and WK.EXIT.MD. | Expose actual E4.a and NAV.MEDIA separately. Optional T.PLAN.RECORD/T.LEAD.SET records a lead but never moves automatically. Media arrival confirms only what is displayed. Back/Return stops old walk. |
| F03 mistaken cancellation | ST.ACCESS.E2 or CY.ACCESS.E2 → UI.SOURCE.POST → T.POST.PHOTO / UI.SOURCE.PHOTO → CY.SOURCE.E3 physical flatten → UI.SOURCE.TEXT. Optional UI.PRESENT.REVIEW → T.PRESENT.DELIVER to ACT.REMY. | First still does not reveal end; cropped photo cannot flatten. Full notice only after secured endpoint. Deliver E3.a/b at Remy approach to correct his account. Unpresented evidence never silently updates him. |
| F04 Media first | ST.EXIT.WK → WK.EXIT.MD → UI.WORLD.IDLE; ACT.LOOP / MD.ACCESS.E8. | No earlier source or form prerequisite. Ari invitation is dismissible; record only displayed account. Loop wake and kit collection independently available. |
| F05 unread portable notes | T.KIT.OPEN → T.KIT.COLLECT → UI.KIT.CARRIED → return Stage → UI.KIT.HANDOFF → T.KIT.SEAT → UI.KIT.SEATED → KIT.NOTE.E6/E7 at ST.RACK.BAY → UI.SOURCE.TEXT. | Collection exposes no note body. Seating preserves both leaflets; mounted Media copies remain. Read once through any legitimate copy then reopen canonical evidence anywhere. |
| F06 one resource first | Dock first gives UI.WORK.NEEDS_KIT; kit first gives UI.WORK.NEEDS_LOOP. Return through existing doors for the missing resource. | No phantom missing-resource success. Following Loop/current caddy host survives travel. Bay handoff at (55,58) precedes rail/control operation. |
| F07 seed-only failure/revision | TILE.FERRY and TILE.PLANT → T.RAIL.COMMIT → T.RUN.REHEARSE → UI.RUN.UNMET. Inspect E6/E7 or ACC.COACH; revise through UI.RAIL.DESTINATIONS. | Ferry transfers seed; Hill does not plant without Pip. Actual order change clears run/certification and resets puppets; no tile lost. Explanation optional. |
| F08 all valid arrangements | UI.WORK.READY → UI.RUN.REHEARSAL → T.RUN.CUE for all cues → T.RUN.FINALIZE → UI.WORK.CERTIFIED. | BPL, FBPL, BFPL, BPFL, BPLF all qualify from resulting state/full completion. Extra Ferry is normal no-op, never an unmet pause/penalty. |
| F09 launch/ending/replay | ST.CONTROL.SHOW → T.SHOW.CHECK → T.RUN.SHOW → UI.RUN.SHOW → T.RUN.FINALIZE → UI.ENDING.CELEBRATION → UI.ENDING.RECAP → T.ENDING.REPLAY. | Current certificate/physical approach required. First completion commits before decorations. Replays retain history; outside Stage replay travels first and awaits deliberate Show action. |
| F10 stop/leave/resume | T.RUN.STOP or T.RUN.LEAVE → UI.RUN.PAUSED or UI.RUN.TERMINAL; door travel; return T.RESUME.OPEN → T.RUN.CONTINUE. | Settle active cue once; retain actual mode, nextCue and order revision. No autoplayer on entry. Hard reload uses durable boundary plus possible-exposure uncertainty. |
| F11 edit after success | UI.WORK.CERTIFIED → UI.RAIL.SELECTED → actual T.RAIL.COMMIT → UI.WORK.READY → T.SHOW.CHECK → UI.WORK.SHOW_CHECK. | New order invalidates certificate. T.RAIL.CANCEL/T.RAIL.NOOP retains it. No old successful order grants current launch. |
| F12 coaching race | UI.COACH.PENDING → T.COACH.WAIT → T.COACH.OFFER → UI.COACH.FALLBACK_OFFER; child keeps playing; T.COACH.FALLBACK or live arrival. | Winner/context rules suppress late/stale response. Focused prepared-hint control is preserved; arrival offers View new reply rather than replacing it. |
| F13 keyboard/compact | 390×844, Largest/Roomier: UI.NAV.OBJECTS/T.NAV.OBJECT, source headings/pickers, KIT collection/seating, UI.RAIL.DESTINATIONS native insert/move/return, Arrange/Watch, Show. | Same physical approaches and source exposure. Text input never moves avatar. Both banks remain visible in Watch, Close/Return reachable with on-screen keyboard; no drag needed. |
| F14 save/recovery/reset | UI.SAVE.PENDING → T.SAVE.FAIL → UI.SAVE.SESSION; reload valid → UI.HOME.SAVED / UI.RESUME.RUN; invalid → UI.RECOVERY.VERSION/DAMAGED. UI.RESET.CASE → T.RESET.CANCEL. | Latest live state survives same-page Home. Unknown read may start unsaved without replacing old record. Explicit replace/reset acceptance only; canceled New game preserves case/preferences. |
| F15 optional Toast | Ignore WK.TOAST, or T.TOAST.START → UI.TOAST.REVEALING → T.TOAST.SETTLE → UI.TOAST.REVEALED, leave via WK.EXIT.MD. | Reveal-at-start durable intent; leave/Skip settles. Magnifier/Replay optional, no required clue/route/certificate dependency. |

B/F/P/L abbreviations above are author-only: Joined Boats/One Boat/Hill/Flower.

## Technical races and checkpoints

Each row specifies initial state → events → required endpoint, observation and recovery. The isolated checker checks the narrow invariants identified in its report; all actual asynchronous browser/service behavior remains future testing.

| ID | Trace | Required result |
|---|---|---|
| X01 collect twice | Media closed kit; collection A queues open→lift. Same action/callback repeats; later fresh Collect after host changed. | Open commit retained if lift canceled. One caddy, four unique tile owners, two canonical note copies. Later action opens current kit, not another pickup. T.KIT.COLLECT/H.KIT. |
| X02 dock twice | Following Loop, closed flap; open commit, cancel docking before contact; retry contact then callback repeats. | Canceled attempt leaves flap open and one following robot on legal floor. Successful retry leaves one docked robot and no Media copy. T.WORLD.COMMIT/H.PHYSICAL. |
| X03 portable identity | Collect unread KIT.NOTE.E6/E7, travel and seat; inspect one; revisit mounted copy. | Ownership never implies reading. Acquired E6/E7 identity is the same at every copy; opening second copy increments exposure, not evidence count. Focus returns to surviving bay/current owner. |
| X04 E2 first still | Open actual post at Stage; frame1/message display; close; open evidence later. | Whole post package available, only actual initial spans/frame exposed. CLIP_LIMIT withheld until later/end information. Photo remains cropped; no E3 grant. |
| X05 planting and revision | F→P from initial state → unmet; read E6 or help; replace order with F→B→P→L. | First result retains Pip left/seed right. Opening help settles before snapshot; revision resets run and resulting full arrangement succeeds. |
| X06 trailing cue | B→P→L→F, first three committed; press Stop. | Successful-looking puppet state, nextCue=3, no certificate. Continue plays harmless F then finalizes. No premature win or extra penalty. |
| X07 final boundary | Same order; F endpoint callback commits, Stop occurs before queued finalization. | nextCue=4, no active cue, paused terminal, no certificate yet. Continue finalizes once without replaying F. Duplicate finalize no-op. |
| X08 controlled inspection | Active Bridge cue; source-opening intent arrives, then old animation callback. | Settle Bridge once, record endpoint, pause before next cue, then source view/focus. Old callback ignored. Closing reader does not continue automatically. |
| X09 rehearsal hard interruption | Marker for active Bridge acknowledged, endpoint not acknowledged; page killed. | Restore prior puppets and next index paused in rehearsal; mark possible previous endpoint exposure. Continue runs only unfinished cue. No independence claim based on rollback. |
| X10 premiere interruption | Certificate exists; show mode at cue1; leave or crash. | Handled leave settles once; crash restores acknowledged show boundary. It remains show, not rehearsal or historical completion. Completion/aftermath only after full successful finalization. |
| X11 edit vs cancel | Successful certificate at order revision8. Select tile/drop outside, then select and actually swap. | Cancel keeps revision8/certificate. Swap commits revision9, clears certificate, resets puppets. Launch rejected with exact rehearsal reason. |
| X12 old acknowledgment | Live revision12, transaction for11 in flight, newest12 queued; ACK11 arrives then transaction12 commits. | ACK11 cannot clear Saving or replace live12. Serial transaction12 makes Saved only at ACK12. Another visit's head change causes explicit replacement, not silent overwrite. |
| X13 quota / unknown read | Save fails while live progress/draft advance; startup read later times out; choose Play without saving; delayed read succeeds. | Continue current session. No unknown/late record overwrite. Retry uses newest state and actual replacement confirmation. |
| X14 incompatible / canceled reset | Saved version999; choose Start a new game then Keep saved game. | Preserve raw record, no fresh case written, preferences unchanged. Existing current-session reset cancellation retains same caseRunId/state. |
| X15 fallback wins | Request A context7 waits8s; choose prepared hint; A arrives valid. | Set winner once, help-displayed for prepared text only, discard A with no duplicate message/ready flag/assistance record. |
| X16 stale live result | Request A at Stage context7; enter Media or expose full notice/edit explanation, context8; A arrives. | No obsolete suggestion displayed. New draft retained. Ask about this version captures new ID/context; an unrelated save ACK would not invalidate A. |
| X17 ready/focus race | Fallback control focused when response arrives; no context change. | Keep that control and offer View new reply. Choosing fallback suppresses held reply. Choosing new reply revalidates, then displays once. |
| X18 service disconnected | Start → Media first → collect/wake → physical bay/dock → inspect optional notes or local direct help → one valid rehearsal → Show → recap. | Zero model requests required; every game outcome unchanged. Prepared/direct assistance is recorded honestly; recap claims only actual performance/recorded wording. |
| X19 stored earlier head | Current record damaged, previous independently valid. | UI.RECOVERY.DAMAGED adds CT.TECH.PREVIOUS/Continue. Continue restores earlier state into a playable unsaved visit with historyUncertain; neither slot changes. Try saving again → UI.RECOVERY.REPLACE; acceptance writes current live progress and preserves the usable previous record, cancel remains unsaved. Back from the original offer preserves both records. |
| X20 missing art/owner | Informative PNG fails or a pre-seat reader invoker no longer exists. | Genuine loading limitation, native named operation/description still usable; unseen image not exposed. Restore focus to current bay/kit owner then Move to fallback. |

## Future execution boundary

Run these against real browser interactions, actual IndexedDB failures, kill/reopen behavior, narrow/zoomed layouts and provider fakes before claiming connected runtime coverage. Follow with authorized live synthetic evaluation. Observe actual children only in the later evaluation phase with appropriate arrangements. Written traces and symbolic checks are not proof the game is enjoyable or teaches a skill.
