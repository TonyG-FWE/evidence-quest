# Native scene-scale review — September 12, 2026

Status: **PASS for the bounded native visual correction**, final renderer `App-qKKSVzYI.js` / `App-CvfCOyJg.css`. This is the parent review of the user's all-room proportion and Stage-outline correction, not acceptance of the entire game or evidence of learning gains.

## Method and candidate identity

Baseline was `e1fbfda4d74a6e6250287954afdcc463657fdb92` / `App-BzEahbjQ`. The four `before-*.jpg` files and their integrity record were captured from a separate saved run by navigating the actual game.

The parent used the in-app browser through visible controls, ordinary room movement, source/kit controls and save continuation. The user's original `127.0.0.1:4175` save was not reset or manipulated. Review origins are parent-owned `localhost:4176`, `127.0.0.1:4177` and `localhost:4177`; the last is a separate fresh case, not a reset of another origin. Native screenshots have their actual desktop viewport dimensions (1265×712 or 1405×790 as captured). Automated 1469×1105 and 320×568 Largest/Roomier cases are separate builder evidence, not native participant tests.

`App-aFsyUvnl` was explicitly a provisional geometry build using the previous density manifest. `App-BK1ZSdTn` supplied the larger source-bounded derivatives plus the Media side approach and Loop-follow correction. The subsequent `App-CVVqWtYR` adds the ResizeObserver repair. Final `App-qKKSVzYI` changes only the small foreground-door area overlapping Loop. Earlier captures remain attributed to their own candidates. Final captures were taken after both native review origins reloaded the final served build.

## Observed results

| Situation | Observation and evidence |
|---|---|
| All four room overviews | The child and crew share a coherent human scale beside furniture and full-height doorframes. The intentionally large Toast invention remains larger than the child. `geometry-{stage,courtyard,workshop,media}.jpg` record the first visual pass; `after-*` captures record the full-density pass. |
| Stage side workstation | The child stands beside the working surface; the case, tile faces and Show/Rehearse controls remain visible. The illustrated table supports connect to the accepted panel. The two extra rectangular outlines are absent; physical teal trim remains. `geometry-stage-side.jpg`. |
| Premiere replay | The saved case reached the workstation, replayed its existing valid arrangement, and returned to the premiere result through ordinary controls on the provisional geometry build. No puzzle result or learning claim was fabricated. |
| Docking with a carried kit | The actual route from Workshop uses the clear right-side dock approach. Loop is visible in its dock, the projection appears and the objective updates. `geometry-dock-clear.jpg`. |
| Kit handoff | Carrying without reading the portable notes, then using Space for the story kit, seats the case at the left side of the raised Stage console and opens the empty arrangement interface. Both portable note actions remain available. |
| Peer conversation | At Ari's physical approach, both children have believable peer proportions and separate bodies. `after-ari-approach.jpg`. |
| Toast interaction | The actual demonstration reveals the small slice under the magnifier; the child does not obscure that consequence. `geometry-toast-side.jpg` and `geometry-toast-revealed.jpg`. |

## Defects found through this review

1. **Mounted Media case covered the enlarged child.** The provisional front stop placed the open case across the child's chest/face. `geometry-media-kit-overlap.jpg` records the defect. Moving mounted Media KIT/TILE operations to `[114,44]` facing left fixes it: `after-media-kit-clear.jpg` shows the child beside the fully visible case, and Collect remains usable. Confirmed fixed in `App-BK1ZSdTn`.
2. **Wake feedback hidden behind the child.** `geometry-carry-loop.jpg` shows the original front wake stop obscuring Loop. Confirmed fixed in final native review: `final-loop-wake.jpg` shows Loop beside the child with the acknowledged wake result; `final-workshop-follow.jpg` shows both clearly after the connected route. Continuous/legal path behavior is separately checked by the builder's contract and browser cases; still images alone do not establish animation cadence.
3. **Raised foreground doorway overlaps standby Loop.** The increased Media frame crossed the robot's upper body/lens because that frame is painted last. `after-media-kit-clear.jpg` preserves the defect. Final `final-media-cutaway.jpg` shows the targeted foreground cutaway revealing Loop's lens/body while the rest of the door remains at full detail. Canonical standby position, doorway access and source meanings are unchanged.
4. **WebKit compact resize feedback.** The first route matrix completed its gameplay but failed the no-page-error assertion with a ResizeObserver delivery-loop error. The builder retained that failure and made a targeted measurement scheduling/unchanged-value repair. The subsequent 21-case matrix passes, including the formerly failing WebKit compact route; final qKKSVzYI Media-only subset passes 6/6 across all three engines. These automated results remain distinct from this native visual pass and the separate performance matrix.

## Final result

Final native inspection found no remaining defect within this bounded scale/outline correction. `final-stage.jpg`, `final-workshop.jpg`, `final-courtyard.jpg` and `final-media-cutaway.jpg` cover all four rooms. `final-stage-side.jpg` shows the raised table, clear working position, readable controls and absence of the two extra outlines. `final-kit-handoff.jpg` and `final-dock-after-kit.jpg` show the fresh kit-first return route: the unread-note kit is seated, Loop docks visibly, the projection appears and the goal updates correctly. Earlier actual docking-first and completed-premiere replay routes are recorded above with their own candidate attribution.

All `final-*.jpg` native captures are 1265×712. Both parent review games were left at Home after the completed observations so they would not add gameplay activity to the builder's isolated performance run. Their saves remain intact. The original user game at port 4175 was untouched during the review.

The builder separately owns the current performance/export evidence and private Git checkpoint. This native pass does not erase historical performance failures, claim unperformed participant validation, or assert that the entire game is complete. The master checklist and runtime BUILD-STATUS retain those broader statuses. `scale-review-20260912/capture-integrity.json` records the exact review images; the final parent import receipt records the authoritative documents and named captures.
