# Evidence Quest project instructions

Read BUILD-STATUS.md first. Continue the next unfinished dependency from docs/design/evidence-quest-design-v3/11-build-packet/implementation-plan.json; do not invent a replacement task sequence.

The first-build authorization paragraphs below are historical. Later explicit user authorization and sections 8–10 of docs/design/evidence-quest-design-v3/13-EXPERIENCE-AND-LITERACY-CORRECTION.md govern the completed ER13/TASK11.17 and production work recorded in BUILD-STATUS.md. At the current checkpoint, TASK11.19 is halted after 1/75 provider attempts on exhausted credit; do not retry or reset its ledger without a coordinated, explicitly authorized resume. TASK11.20/.21 and ER13.06–08 remain open for their recorded qualification gaps. The current private checkpoint authorizes no further runtime changes or provider requests by itself.

The complete imported design package is the product authority: v3 story/literacy, Item 05 geometry/physical actions, 06 interfaces, 07 canonical words, 08 illustrated finish/current cast, 09 technical contracts, 10 individual assets, and 11/12 task scope/readiness. docs/BUILD-START.md authorizes TASK11.01–16 plus a new private GitHub repository and normal project pushes. Historical absolute paths are provenance, not permission to access other projects.

All game work stays in this dedicated workspace. Preserve the original imported bytes using evidence/design-import.json; document any later source/status change and retain dated evidence. No proprietary project access, nested checkout, force push, global identity change, deployment, public publication, paid live evaluation, child-live activation, production art, or submission is authorized.

Use the prescribed TypeScript/React/Canvas2D/Node stack, native accessible controls, a single immutable store and serialized coordinator, and IndexedDB. Keep exact source components/exposure separate from possession, records, NPC delivery, assistance and completion. No answer gates, mandatory mistakes, clue counts or inferred learning claims. All five successful arrangements are equal. Temporary components never replace the final illustrated-quality requirement.

Maintain BUILD-STATUS.md by existing TASK11 IDs with results, exact FIX11/CHECK11 evidence, limitations, commit references and next dependency. Test real handlers and connected browser behavior. Synthetic fault seams must be labeled; live/player/production checks remain NOT_RUN when excluded. The seven deferred states are only those explicitly listed in FIRST-CONNECTED-BUILD.md.

Required commands: npm ci; npm run validate:content; npm run check; npm run build:server; npm run test:contracts; npm run eval:coach -- --mode authored; npm run build; npm run test:browser. These require implemented scripts and a reviewed lockfile; do not replace their required behavior with stubs. Authored operation must not need secrets. Use project-owned loopback ports and stop only owned processes.

Use Context7 current documentation for library/API/CLI setup and syntax. Local engineering and business-rule logic follow the frozen contracts. No subagent delegation is authorized by this file.
